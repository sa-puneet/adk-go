# sequential

**Parent**: [workflowagents](../AGENTS.md) | **Repository**: An open-source, code-first Go

> Orchestrates the creation and execution of a sequential agent workflow, defining a custom agent that generates language model responses. It functions as the main entry point for launching multiple sub-agents within a comprehensive agent framework.

## Key Components

*No components detected*

## Folder Overview

## See Also

- [Parent overview →](../AGENTS.md) - Repository-level concepts and architecture
- [loop/ →](../loop/AGENTS.md) - Orchestrates the execution of a custom agent within an iterative loop, defining its behavior to produce specific LLM responses. Initializes the application's execution context and processes command-line arguments to launch configured agents.
- [parallel/ →](../parallel/AGENTS.md) - Provides the main entry point for an application that orchestrates the creation and parallel execution of composite agents. Initializes individual sub-agents and integrates them into a concurrent processing structure.
- [sequentialCode/ →](../sequentialCode/AGENTS.md) - Implements the main entry point for a multi-stage AI agent pipeline, orchestrating the initialization of a Gemini model and constructing a sequence of specialized agents for code generation and refinement. Provides the core mechanism for launching these agents to process user requests in a sequential workflow.
